#include "Grid.h"

SCENARIO( "Adding and moving items in a grid", "[grid]" ) {
  GIVEN("An empty grid") {
    WHEN("we iterate over the whole grid") {
      THEN("it appears empty") {

      }
    }
    WHEN("we check each grid box") {
      THEN("each box appears empty") {

      }
    }
  }
}
